# searchKnowledgeBase has moved to python-adtech-mcp-client.
# It is a native LangChain StructuredTool (app/agent/tools.py) that calls
# Vertex AI Search (Discovery Engine) directly — no HTTP hop to the server.
# The MCP server has zero AI / GCP SDK dependencies by design.
